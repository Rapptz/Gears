language: cpp
compiler:
    - gcc

branches:
    only:
        - master

before_install:
    - sudo apt-get --yes install g++-4.8

before_script:
    - export CXX="g++-4.8"

script: scons runtest